{
  "name": "sketchdraw-monorepo",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "dev": "pnpm -r --parallel dev",
    "dev:website": "pnpm --filter @sketchdraw/website dev",
    "build:website": "pnpm --filter @sketchdraw/website build"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
