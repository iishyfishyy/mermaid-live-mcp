---
const npmInstall = `npm install -g @sketchdraw/cli`;
const mcpConfig = `{
  "mcpServers": {
    "sketchdraw": {
      "command": "npx",
      "args": ["-y", "@sketchdraw/mcp-server"]
    }
  }
}`;
---

<section id="install" class="section">
  <div class="container">
    <h2 class="section-title">Get started in seconds</h2>
    <p class="section-subtitle">Install the CLI or add the MCP server to your AI tool.</p>
    <div class="install-grid">
      <div class="install-card">
        <h3 class="install-card-title">CLI</h3>
        <p class="install-card-desc">Convert Mermaid files to hand-drawn SVGs from the command line.</p>
        <pre><code>{npmInstall}</code></pre>
      </div>
      <div class="install-card">
        <h3 class="install-card-title">MCP Server</h3>
        <p class="install-card-desc">Add to your Claude Desktop or Cursor MCP configuration:</p>
        <pre><code>{mcpConfig}</code></pre>
      </div>
    </div>
  </div>
</section>

<style>
  .install-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 1.5rem;
  }

  .install-card {
    background: var(--color-bg-card);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
    padding: 2rem;
  }

  .install-card-title {
    font-size: 1.25rem;
    font-weight: 600;
    margin-bottom: 0.5rem;
  }

  .install-card-desc {
    color: var(--color-text-muted);
    font-size: 0.9rem;
    margin-bottom: 1.25rem;
    line-height: 1.6;
  }

  .install-card pre {
    font-size: 0.825rem;
  }

  @media (max-width: 768px) {
    .install-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
